spring:
  application:
    name: nextstation
  profiles:
    active: local
  jpa:
    hibernate:
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO
    com.cotato.nextstation: DEBUG

server:
  port: 8080